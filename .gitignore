__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.ruff_cache/
state/
data_cache/
logs/
# ...but NOT the dashboard "Logs & Alerts" route, which lives under a logs/ dir
!/dashboard/frontend/src/app/(dash)/logs/
!/dashboard/frontend/src/app/(dash)/logs/**
*.parquet
*.duckdb
.mcp.json.txt
.mcp.json
settings.json.txt
*.png
.claude/settings.local.json

# dashboard
node_modules/
.next/
out/
dist/
*.tsbuildinfo
.env.*
!.env.example
dashboard/backend/.env
secrets/
pgdata/
backups/*.dump

# deployment report contains live credentials — NEVER commit
VPS_DEPLOYMENT_REPORT.md
VPS_DEPLOYMENT_REPORT.*

# Rendered exports (PDF/DOCX) can carry the same secrets as their source doc —
# never track them. Force a specific safe file with `git add -f <file>`.
*.pdf
*.docx

# VPS-local: DB dumps and host-specific compose override
deploy/backups/
deploy/docker-compose.override.yml
